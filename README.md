# Валентинка 💝

Небольшое веб-приложение в подарок на 14 февраля — карусель фото, вопрос «Будешь моей валентинкой?» и конфетти, если нажать «Да».

## Что внутри

- **Карусель фотографий** — листаешь свайпом или стрелками, фото переключаются с анимацией
- **Вопрос с убегающей кнопкой** — «Нет» уезжает в сторону при наведении
- **Саркастический экран** — если всё же нажать «Нет»
- **Финалка с конфетти** — после «Да» показывается финальное фото и всё сыпется конфетти

Всё адаптивное, работает на телефоне и на компе.

## Как запустить

```bash
git clone https://github.com/kirill-korotkov/valentine.git
cd valentine

npm install
npm run dev
```

## Запуск через Docker (OrbStack)

```bash
docker compose up --build
```

Открой http://localhost:5173. При изменении кода HMR обновит страницу.

## Сборка iOS-приложения (Capacitor)

Нужен Mac с Xcode. Фото положи в `public/photos/` перед сборкой.

```bash
npm run ios
```

Откроется Xcode — выбери симулятор или подключи iPhone и нажми Run.

**Подробный план** (от симулятора до App Store): [docs/IOS_PLAN.md](docs/IOS_PLAN.md)
