# 💝 Валентинка - Романтическое веб-приложение

Интерактивное одностраничное приложение для признания на День Святого Валентина с каруселью фотографий, анимациями и сюрпризами!

## 🎨 Особенности

- **Карусель фотографий** - Красивая презентация ваших совместных фото с плавными переходами
- **Интерактивный вопрос** - Кнопка "Нет" убегает от курсора с забавными анимациями
- **Эффектные переходы** - Плавные анимации между экранами с использованием Motion (Framer Motion)
- **Конфетти** - Праздничный эффект конфетти при положительном ответе
- **Адаптивный дизайн** - Отлично работает на ПК и мобильных устройствах
- **Яркая цветовая гамма** - Теплые красные, оранжевые и янтарные тона

## 🚀 Быстрый старт

### Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/kirill-korotkov/valentine.git
cd valentine

# Установите зависимости
npm install
# или
pnpm install
# или
yarn install
```

### Запуск в режиме разработки

```bash
npm run dev
# или
pnpm dev
```

Откройте [http://localhost:5173](http://localhost:5173) в браузере.

### Сборка для production

```bash
npm run build
# или
pnpm build
```

Готовые файлы будут в папке `dist/`.

## ⚙️ Настройка контента

Все тексты и пути к фотографиям настраиваются в файле **`/src/app/config.ts`**:

```typescript
export const config = {
  // Пути к фотографиям для стартовой карусели
  startPhotos: [
    "https://images.unsplash.com/photo-1...",
    "https://images.unsplash.com/photo-2...",
    // Добавьте свои фото
  ],

  // Финальное фото
  finalPhoto: "https://images.unsplash.com/photo-...",

  // Тексты
  texts: {
    question: "Будешь моей валентинкой?",
    buttonYes: "Да! ❤️",
    buttonNo: "Нет",
    sarcastic: "Ага, конечно... 🙄",
    finalTitle: "Ура! 🎉",
    finalMessage: "Я так счастлив(а)! С днём Святого Валентина!",
    finalDate: "14 февраля 2026",
    finalSignature: "С любовью, твоя валентинка",
    buttonAgain: "Ещё раз ❤️"
  },

  // Настройки таймингов
  timings: {
    photoAutoAdvance: 3000, // автоматическое переключение фото (мс)
    sarcasticScreenDuration: 2000 // длительность саркастического экрана (мс)
  }
};
```

### Как добавить свои фотографии

1. **Вариант 1**: Загрузите фото в папку `/public/images/` и используйте относительные пути:
   ```typescript
   startPhotos: [
     "/images/photo1.jpg",
     "/images/photo2.jpg",
   ]
   ```

2. **Вариант 2**: Используйте URL изображений (например, из облачного хранилища):
   ```typescript
   startPhotos: [
     "https://your-cloud-storage.com/photo1.jpg",
     "https://your-cloud-storage.com/photo2.jpg",
   ]
   ```

## 📦 Деплой на GitHub Pages

### Автоматический деплой с GitHub Actions

1. Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: ['main']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
      
      - name: Install dependencies
        run: pnpm install
      
      - name: Build
        run: pnpm build
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

2. Включите GitHub Pages в настройках репозитория:
   - Перейдите в **Settings** → **Pages**
   - В разделе **Source** выберите **GitHub Actions**

3. Запушьте изменения:
```bash
git add .
git commit -m "Add deployment workflow"
git push origin main
```

4. Ваше приложение будет доступно по адресу:
   **https://kirill-korotkov.github.io/valentine/**

### Ручной деплой

```bash
# Соберите проект
npm run build

# Установите gh-pages (если еще не установлен)
npm install -g gh-pages

# Опубликуйте
gh-pages -d dist
```

## 🌐 Альтернативные платформы для деплоя

### Vercel (рекомендуется)

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Задеплойте:
```bash
vercel
```

Ссылка: [vercel.com](https://vercel.com)

### Netlify

1. Перетащите папку `dist` на [Netlify Drop](https://app.netlify.com/drop)
2. Или используйте CLI:
```bash
npm install -g netlify-cli
netlify deploy --prod
```

## 🛠️ Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик и dev-сервер
- **Tailwind CSS v4** - Стилизация
- **Motion (Framer Motion)** - Анимации
- **Canvas Confetti** - Эффект конфетти
- **Lucide React** - Иконки

## 📝 Структура проекта

```
valentine/
├── src/
│   ├── app/
│   │   ├── App.tsx              # Главный компонент с логикой навигации
│   │   ├── config.ts            # Конфигурация (тексты, фото, настройки)
│   │   └── components/
│   │       ├── PhotosScreen.tsx     # Экран с каруселью фото
│   │       ├── QuestionScreen.tsx   # Экран с вопросом
│   │       ├── SarcasticScreen.tsx  # Саркастический экран
│   │       ├── FinalScreen.tsx      # Финальный экран с конфетти
│   │       └── figma/
│   │           └── ImageWithFallback.tsx  # Компонент для изображений
│   ├── styles/
│   │   ├── fonts.css            # Шрифты
│   │   └── theme.css            # Тема и CSS переменные
│   └── main.tsx                 # Точка входа
├── public/                      # Статические файлы
├── package.json
├── vite.config.ts
└── README.md
```

## 🎯 Экраны приложения

1. **PhotosScreen** - Карусель с вашими фотографиями
2. **QuestionScreen** - Вопрос "Будешь моей валентинкой?" с убегающей кнопкой "Нет"
3. **SarcasticScreen** - Появляется при нажатии на "Нет"
4. **FinalScreen** - Праздничный экран с конфетти при выборе "Да"

## 💡 Советы по использованию

- Добавьте 3-5 фотографий для стартовой карусели
- Используйте качественные изображения (рекомендуется минимум 1920x1080px)
- Измените тексты под себя в `config.ts`
- Настройте таймеры автоматического переключения под ваши предпочтения

## 📄 Лицензия

MIT License - используйте свободно для личных целей!

## ❤️ Автор

Создано с любовью для особенного человека 💕

---

**Желаю вам чудесного Дня Святого Валентина!** 🌹
